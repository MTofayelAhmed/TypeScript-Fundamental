// let userNames:string[];
let userNames:Array<string>;
userNames = ["a", "b", "c"]

console.log(userNames[0])

// array can be multiple types 

let multipleTypes:(string |number)[]
multipleTypes= [0, "11"]
console.log(multipleTypes.length)